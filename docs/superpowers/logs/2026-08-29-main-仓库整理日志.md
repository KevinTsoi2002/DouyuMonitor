# 2026-08-29 main 仓库整理日志

## 目标

将 `main` 明确为唯一维护的 Qt Quick/QML + C++ + libmpv 产品分支，并清理迁移后残留的 Electron/TypeScript 运行时代码、本地构建缓存和诊断输出。

## 清理前审计

| 范围 | 结论 |
| --- | --- |
| 当前产品运行时 | `native` 是唯一受维护的 Qt 原生实现。 |
| 旧运行时 | 根目录 `src`、`tests`、`scripts`、Node/Electron/Vite 配置及 StreamGet 根目录脚本均属于旧 Electron/TypeScript 框架。 |
| 历史保留 | 旧框架完整代码、计划和设计文档均由 `legacy-framework` 分支保留。 |
| 本地产物 | 本次删除未跟踪诊断输出和生成的 `native/dependencies.lock.json`；`native/out`、`native/sdk`、`native/.venv`、`native/dependencies` 与 `native/vcpkg_installed` 保留为本机可复现构建环境，但由 Git 忽略。 |

## 文档调整

- 根 `README.md` 已改为中文 Qt-only 使用说明，包含 V0.2.0、构建、安装包、服务边界和验证要求。
- 新增 `docs/文件职责索引.md`，逐项说明 main 中受维护的根目录、构建、QML、C++、服务、测试和文档文件职责。
- 本日志使用中文，后续日志继续采用中文并记录计划匹配、验证结果和未完成事项；`2026-08-24` 的 Qt 迁移日志保留原始英文审计内容。

## 清理边界

- 删除 `main` 中的旧 Electron/TypeScript 运行时代码和其测试、构建配置、Node 依赖清单及旧 StreamGet 桥接脚本。
- 删除 `main` 中日期为 2026-08-07 至 2026-08-19 的 Electron 计划和设计文档。
- 删除当前工作树未跟踪的诊断文本和生成依赖锁定文件；保留可复现 Qt 开发需要的 SDK、依赖缓存和构建输出。
- 保留 Qt 原生源码、测试、构建/安装脚本，以及 2026-08-24 之后的 Qt 设计、计划和日志。
- 不删除 `legacy-framework` 分支；该分支是旧框架的可追溯备份。

## 验证记录

- 使用 Visual Studio 2022 x64 工具链重新配置 Release，CMake 生成成功。
- `cmake --build --preset windows-x64-release --target douyu_monitor_native` 成功，Qt 部署已刷新到被 Git 忽略的本地构建目录。
- `ctest --preset windows-x64-release` 通过 `28/28`，总耗时 `84.21` 秒。
- `out\\build\\windows-x64-release\\douyu_monitor_native.exe --self-test` 输出 `native self-test passed: Qt Quick renderer` 并以 `0` 退出。
- `WrapVulkanHeaders` 缺少 Vulkan 头文件的提示来自可选 Qt 包装模块，不影响当前 OpenGL + libmpv Release 构建和测试。
- 暂存树顶层仅包含 `.gitignore`、`README.md`、`docs` 和 `native`；旧运行时代码和 2026-08-24 前文档的检查结果均为空。
- `git diff --cached --check` 通过，且文件职责索引覆盖当前受跟踪文件的检查结果为 `0` 缺失项。

## 计划匹配

- 当前工作树只保留 Qt 原生运行时代码、构建脚本、当前 Qt 设计/计划和中文整理日志，符合 Qt-only `main` 的产品边界。
- 旧 Electron/TypeScript 源码和 2026-08-24 前的设计、计划已从 `main` 删除，`legacy-framework` 分支继续作为完整历史副本。
- README 的构建、启动、服务和安装器命令已统一为在 `native` 目录执行；文件职责索引覆盖根目录、原生模块、测试和当前文档。

## 下一步

1. 创建整理提交并推送 `main`。
