# 更新日志

## v0.1.4 - 2026-08-19

### 播放稳定性

- 修复多直播间场景中，部分房间已经获取播放源但画面保持黑屏的问题。
- 播放恢复以首个解码帧为准；10 秒内没有首帧时触发自动恢复。
- 关闭 mpegts.js Worker 和 MSE Worker，避免 Electron 的 `file://` 打包环境出现 Worker 初始化失败。
- 调整直播缓冲与同步参数，降低多路播放时的频繁卡顿和重连。

### 多路监看

- 修复八路以上布局中，右上角“更多操作”菜单被直播画面裁切的问题。
- 菜单会根据窗口边界自动向上或向下展开，并在调整窗口大小或滚动时重新定位。
- 支持点击菜单外区域或按 `Esc` 关闭菜单。

### 声音与工作区

- 多声道模式下每个房间的静音状态会独立保存和恢复。
- 工作区与工作区预设会保存房间静音列表，切换预设后保持对应声音状态。

### 发布说明

- 提供 Windows x64 安装包、Blockmap 和 SHA-256 校验文件。
- 当前安装包未使用可验证的 Windows 代码签名证书。请从 GitHub Release 下载，并核对 SHA-256 校验值。
